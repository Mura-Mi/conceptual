#!/usr/bin/env ruby

require 'conceptual'

Dir[File.join(Dir.pwd, 'concepts', 'models', '**', '*.rb')].each { |d|
  File.open(d) do |file|
    require_relative file
    puts Object.const_get(File.basename(file).gsub('.rb', '').to_pascal)
  end
}


